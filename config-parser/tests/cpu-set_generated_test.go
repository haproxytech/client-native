// Code generated by go generate; DO NOT EDIT.
/*
Copyright 2019 HAProxy Technologies

Licensed under the Apache License, Version 2.0 (the "License");
you may not use this file except in compliance with the License.
You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.
*/

package tests

import (
	"fmt"
	"strings"
	"testing"

	"github.com/haproxytech/client-native/v6/config-parser/parsers"
)

func TestCPUSet(t *testing.T) {
	tests := map[string]bool{
		"cpu-set reset":                         true,
		"cpu-set reset # some comment":          true,
		"cpu-set drop-cpu 1 # some comment":     true,
		"cpu-set drop-cpu 1,3":                  true,
		"cpu-set drop-cpu 0-5":                  true,
		"cpu-set only-cpu 1 # some comment":     true,
		"cpu-set only-cpu 1,3":                  true,
		"cpu-set only-cpu 0-5":                  true,
		"cpu-set drop-node 1 # some comment":    true,
		"cpu-set drop-node 1,3":                 true,
		"cpu-set drop-node 0-5":                 true,
		"cpu-set only-node 1 # some comment":    true,
		"cpu-set only-node 1,3":                 true,
		"cpu-set only-node 0-5":                 true,
		"cpu-set drop-cluster 1 # some comment": true,
		"cpu-set drop-cluster 1,3":              true,
		"cpu-set drop-cluster 0-5":              true,
		"cpu-set only-cluster 1 # some comment": true,
		"cpu-set only-cluster 1,3":              true,
		"cpu-set only-cluster 0-5":              true,
		"cpu-set drop-core 1 # some comment":    true,
		"cpu-set drop-core 1,3":                 true,
		"cpu-set drop-core 0-5":                 true,
		"cpu-set only-core 1 # some comment":    true,
		"cpu-set only-core 1,3":                 true,
		"cpu-set only-core 0-5":                 true,
		"cpu-set drop-thread 1 # some comment":  true,
		"cpu-set drop-thread 1,3":               true,
		"cpu-set drop-thread 0-5":               true,
		"cpu-set only-thread 1 # some comment":  true,
		"cpu-set only-thread 1,3":               true,
		"cpu-set only-thread 0-5":               true,
		"---":                                   false,
		"--- ---":                               false,
	}
	parser := &parsers.CPUSet{}
	for command, shouldPass := range tests {
		t.Run(command, func(t *testing.T) {
			line := strings.TrimSpace(command)
			lines := strings.SplitN(line, "\n", -1)
			var err error
			parser.Init()
			if len(lines) > 1 {
				for _, line = range lines {
					line = strings.TrimSpace(line)
					if err = ProcessLine(line, parser); err != nil {
						break
					}
				}
			} else {
				err = ProcessLine(line, parser)
			}
			if shouldPass {
				if err != nil {
					t.Error(err)
					return
				}
				result, err := parser.Result()
				if err != nil {
					t.Error(err)
					return
				}
				var returnLine string
				if result[0].Comment == "" {
					returnLine = result[0].Data
				} else {
					returnLine = fmt.Sprintf("%s # %s", result[0].Data, result[0].Comment)
				}
				if command != returnLine {
					t.Errorf("error: has [%s] expects [%s]", returnLine, command)
				}
			} else {
				if err == nil {
					t.Errorf("error: did not throw error for line [%s]", line)
				}
				_, parseErr := parser.Result()
				if parseErr == nil {
					t.Errorf("error: did not throw error on result for line [%s]", line)
				}
			}
		})
	}
}
